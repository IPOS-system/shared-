# Shared Repository

This repository contains the shared interfaces and DTO/model classes used by the IPOS subsystems:

- IPOS-SA
- IPOS-CA
- IPOS-PU

## Purpose

The shared repo defines the contracts between subsystems so that all teams use the same method names, parameters, and return types.
